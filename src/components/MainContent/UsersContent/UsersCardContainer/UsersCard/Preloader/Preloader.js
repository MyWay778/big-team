import React from "react"
import classes from './Preloader.module.css'

const Preloader = props => {
    return (
        <div className={`${classes.ldsDualRing} preloader`}></div>
    )
}

export default Preloader